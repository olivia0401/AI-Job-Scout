# AI Job Scout 运行依赖
flask>=3.0
requests>=2.31
openpyxl>=3.1
pypdf>=4.0        # 解析 PDF 简历（extract_resume_text）

# 简历修改建议（可选）：在工具里用 Claude 生成，需配 anthropic_api_key
anthropic>=0.40        # 仅“生成简历修改建议”功能用到；不用可不装

# 开发/测试（可选）：pytest 跑 tests/，scipy 跑 eval/ 的排名相关性
# pip install pytest scipy
