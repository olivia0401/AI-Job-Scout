# Personal data: full resume text, scan results, caches — never upload
data/

# Exported result spreadsheets (contain personal job-search data)
*.xlsx

# Python
__pycache__/
*.pyc
.venv/
venv/

# Cloud credentials / sensitive files — never upload (AWS access keys, etc.)
*.csv

# Installers / large binaries that don't belong in the repo
*.exe

# Local deployment runbook (contains server IP / personal infra steps)
如何部署说明.md

# Dev-tool artifacts
*.spec
build/
dist/
